# Campaign Handoff

## Instagram Caption

CSolutions built MarkdownToHTML Converter to make a common handoff simpler.

Paste or upload Markdown, preview the formatted result, inspect the generated
HTML, then copy what you need or download a complete document. It works from a
desktop or phone, does not require an account, and the source is available
publicly on GitHub.

Try it at mdtohtmlconverter.com.

Built and maintained by CSolutions.

## Short CTA

Convert your next Markdown file at mdtohtmlconverter.com.

## Slide Alt Text

1. Dark CSolutions launch graphic with the headline "Markdown becomes HTML"
   above a focused view of the real MarkdownToHTML conversion workspace.
2. Split before-and-after graphic showing publish-ready Markdown beside the
   converter's real rendered preview and the line "Preview first."
3. Source-inspection graphic showing the real generated HTML panel beside the
   headline "The source isn't hidden."
4. Full mobile MarkdownToHTML interface beside the headline "Your browser is
   enough" and a short list of the converter's practical actions.
5. Solid-mint launch card featuring the complete mdtohtmlconverter.com domain
   on one line, a real converter screenshot, "Free to use. No sign-in
   required," and an invitation to open the converter.

The five TikTok slides use the same sequence and alt text. Their layouts are
recomposed for a `1080x1920` canvas, with key labels kept above and left of the
platform's caption and action controls.

## Motion Concept

Create an 8–10 second vertical product walkthrough from the Story cover:

1. Fade in the CSolutions mark and headline over 350ms.
2. Move the real product capture upward by no more than 6% over 2.5 seconds.
3. Reveal `WRITE`, `PREVIEW`, and `EXPORT` one at a time with 180ms opacity
   transitions.
4. Cut to the domain and `FREE · NO SIGN-IN` for the final 2 seconds.

Use ease-out movement, no dramatic zooms, and no fabricated cursor interactions.

## Visual System

- Canvas: `#eef0ec`
- Surface: `#f7f8f4`
- Primary ink: `#17231d`
- Muted text: `#526158`
- Accent: `#176b48`
- Accent dark: `#0e5035`
- Supporting mint: `#b9d9c6`
- Supporting warm neutral: `#efe9dc`
- Type: Arial/Helvetica for display and Courier New for technical labels
- Carousel safe margin: 64px
- Story safe margin: 70px, with CTA above the bottom 150px
- TikTok safe area: critical text, domains, logos, and CTA controls stay inside
  a conservative safe frame of `left: 90px`, `right: 220px`, `top: 180px`, and
  `bottom: 420px`
- Brand treatment: publish from the CSolutions account and describe the tool as
  a CSolutions product
- Effects: solid color fills only; no gradients, glass, translucent overlays,
  glow effects, or decorative color blending

## Screenshot Fit QA

- All carousel slides export at `1080x1350`; the Story/Reel cover exports at
  `1080x1920`.
- TikTok slides export at `1080x1920` as `tiktok-1.png` through
  `tiktok-5.png`.
- TikTok safe-zone proof images export as `tiktok-safe-check-1.png` through
  `tiktok-safe-check-5.png`, with the review sheet
  `tiktok-safe-zone-check.png`.
- Screenshot proportions are preserved. No product image is stretched.
- Slides 1, 2, 3, 5, and the Story cover use deliberate close crops to keep the
  converter workflow readable at social-media size.
- Slide 4 uses `object-fit: contain` so the complete mobile workflow remains
  visible.
- The important interface controls, Markdown input, rendered preview, and HTML
  source remain visible in their respective compositions.
- Screenshots may intentionally extend outside the TikTok safe frame, but
  posting-critical text does not.

## Export Settings

- Carousel: PNG, sRGB, `1080x1350`, no additional compression
- Story/Reel cover: PNG, sRGB, `1080x1920`, no additional compression
- TikTok carousel: PNG, sRGB, `1080x1920`, no additional compression
- Preserve text layers and screenshot placement in `promo.html` and `promo.css`
